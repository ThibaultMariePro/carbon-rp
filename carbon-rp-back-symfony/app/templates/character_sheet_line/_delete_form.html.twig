<form method="post" action="{{ path('app_character_sheet_line_delete', {'id': character_sheet_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ character_sheet_line.id) }}">
    <button class="btn">Delete</button>
</form>
