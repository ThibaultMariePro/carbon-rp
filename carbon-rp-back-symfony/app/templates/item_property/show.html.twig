{% extends 'base.html.twig' %}

{% block title %}ItemProperty{% endblock %}

{% block body %}
    <h1>ItemProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_property.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_property.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ item_property.value }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ item_property.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_item_property_index') }}">back to list</a>

    <a href="{{ path('app_item_property_edit', {'id': item_property.id}) }}">edit</a>

    {{ include('item_property/_delete_form.html.twig') }}
{% endblock %}
