<form method="post" action="{{ path('app_item_property_delete', {'id': item_property.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_property.id) }}">
    <button class="btn">Delete</button>
</form>
