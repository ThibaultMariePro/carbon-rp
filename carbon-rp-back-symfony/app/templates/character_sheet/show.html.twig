{% extends 'base.html.twig' %}

{% block title %}CharacterSheet{% endblock %}

{% block body %}
    <h1>CharacterSheet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ character_sheet.id }}</td>
            </tr>
            <tr>
                <th>PageNumber</th>
                <td>{{ character_sheet.pageNumber }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ character_sheet.createdAt ? character_sheet.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ character_sheet.updatedAt ? character_sheet.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_character_sheet_index') }}">back to list</a>

    <a href="{{ path('app_character_sheet_edit', {'id': character_sheet.id}) }}">edit</a>

    {{ include('character_sheet/_delete_form.html.twig') }}
{% endblock %}
